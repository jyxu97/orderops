# Test defaults. Loaded alongside the main application.yml (.properties wins on conflicts),
# so this file overrides only what tests need to differ.

# The Redis pub/sub bridge subscribes eagerly on startup. Tests mock StringRedisTemplate but
# have no Redis server, so the bridge stays off; tests that exercise it drive the broadcaster
# directly instead.
realtime.redis-bridge.enabled=false
